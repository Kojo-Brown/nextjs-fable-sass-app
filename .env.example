# Postgres connection string
DATABASE_URL=postgres://localhost:5432/fable_jobs

# Anthropic API key for AI resume matching (optional — feature degrades gracefully without it)
ANTHROPIC_API_KEY=

# Secret used by the job-runner endpoint (generate with: openssl rand -hex 32)
JOBS_RUNNER_SECRET=
